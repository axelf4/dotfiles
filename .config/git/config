[user]
	name = Axel Forsman
	email = axel@axelf.se
[push]
	default = simple
[pull]
	ff = only
[merge]
	conflictStyle = zdiff3

[diff "lisp"]
	xfuncname = "^\\([^[:space:]]*def[^[:space:]]+[[:space:]]+[^()[:space:]]+.*$"

[url "git@github.com:"]
	insteadOf = "gh:"
